streamlit>=1.35.0
sentence-transformers>=3.0.0
scikit-learn>=1.4.0
pdfplumber>=0.11.0
pandas>=2.0.0
numpy>=1.26.0
openai>=1.30.0        # used for both OpenAI and Groq (same SDK, different base_url)
torch>=2.2.0          # required by sentence-transformers (CPU-only is fine)
